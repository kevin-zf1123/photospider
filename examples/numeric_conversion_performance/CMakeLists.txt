add_executable(photospider_numeric_conversion_performance main.cpp)
target_link_libraries(photospider_numeric_conversion_performance PRIVATE Photospider::kernel)
target_compile_features(photospider_numeric_conversion_performance PRIVATE cxx_std_17)

option(PHOTOSPIDER_BUILD_NUMERIC_CONVERSION_SME_EXPERIMENT "Build the standalone Apple SME conversion experiment" OFF)
if(PHOTOSPIDER_BUILD_NUMERIC_CONVERSION_SME_EXPERIMENT)
  if(NOT APPLE OR NOT CMAKE_SYSTEM_PROCESSOR MATCHES "^(arm64|aarch64)$")
    message(FATAL_ERROR "The numeric conversion SME experiment requires Apple arm64")
  endif()
  add_executable(photospider_numeric_conversion_sme_experiment sme_experiment.cpp sme_kernels.cpp)
  target_compile_features(photospider_numeric_conversion_sme_experiment PRIVATE cxx_std_17)
  target_compile_options(photospider_numeric_conversion_sme_experiment PRIVATE -fno-fast-math -frounding-math -ffp-contract=off)
  set_source_files_properties(sme_kernels.cpp PROPERTIES COMPILE_OPTIONS "-march=armv8-a+sme+sme-f64f64")
endif()
