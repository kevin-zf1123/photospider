cmake_minimum_required(VERSION 3.21)
project(PhotospiderFftWorkflow LANGUAGES CXX)
if(NOT TARGET Photospider::kernel)
  find_package(Photospider 0.15 CONFIG REQUIRED COMPONENTS kernel)
endif()
add_executable(photospider_fft_workflow main.cpp)
target_link_libraries(photospider_fft_workflow PRIVATE Photospider::kernel)
