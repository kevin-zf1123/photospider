cmake_minimum_required(VERSION 3.21)
project(PhotospiderRepresentationsWorkflow LANGUAGES CXX)
if(NOT TARGET Photospider::kernel)
  find_package(Photospider 0.13 CONFIG REQUIRED COMPONENTS kernel)
endif()
add_executable(photospider_representations_workflow main.cpp)
target_link_libraries(photospider_representations_workflow PRIVATE Photospider::kernel)
