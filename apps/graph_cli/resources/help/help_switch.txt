switch <name> [c]

Switch current graph session to <name>.
With 'c', save the current session before copying its YAML/config into <name>.
If that save fails, abort before creating or loading the target session.
