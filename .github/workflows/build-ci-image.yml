name: Build CI Image

# LEGACY / DISABLED-BY-DEFAULT:
# This workflow is retained as manual scaffolding only. It currently sets
# push: false and does not publish a maintained CI image. Do not treat it as an
# active validation or release path until a future change revalidates and
# documents the container workflow end to end.
on:
  # Manual-only TODO scaffolding. Re-enable automatic image publishing only
  # after the container path is validated and documented as maintained CI.
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/ci-scheduler
          tags: |
            type=raw,value=latest
            type=sha

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.ci
          push: false
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
